export enum IssuedPassFilterOptions {
  LATEST = 'LATEST',
  HIGHEST_PRICE = 'HIGHEST_PRICE',
  BEST_SELLING = 'BEST_SELLING',
}

export enum PassStatusOptions {
  AVAILABLE = 'AVAILABLE',
  DISABLED = 'DISABLED',
}
