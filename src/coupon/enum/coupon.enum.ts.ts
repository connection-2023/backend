export enum UserCouponStatusOptions {
  AVAILABLE = 'AVAILABLE',
  USED = 'USED',
  EXPIRED = 'EXPIRED',
}

export enum CouponFilterOptions {
  LATEST = 'LATEST',
  UPCOMING = 'UPCOMING',
}

export enum IssuedCouponStatusOptions {
  AVAILABLE = 'AVAILABLE',
  DISABLED = 'DISABLED',
}
